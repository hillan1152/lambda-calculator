import React from "react";
import { className } from "postcss-selector-parser";

export default const SpecialButton = () => {
  return (
    <>
      {/* Display a button element rendering the data being passed down from the parent container on props */}
    </>
  );
};
